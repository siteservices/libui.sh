#!/usr/bin/env libui
#####
#
# test_NROPT
#
#####

test_NROPT () {
  LibuiGetDisplayTestValues
  ${_Test_debug} && local p=3 || local p=2
  LibuiPerformTest 'Tell -- "${NROPT}"'
  LibuiValidateTest ${?} 0 "${p}"
  return ${?}
}

# test setup
_Test_opt+='-x P '

return 0
