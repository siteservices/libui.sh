#!/usr/bin/env libui
#####
#
# test_SH
#
#####

test_SH () {
  ${ZSH} && local sh="${commands[zsh]}" || local sh="${BASH:-sh}"
  LibuiPerformTest 'Tell -- "${SH}"'
  LibuiValidateTest ${?} 0 "${sh}"
  return ${?}
}

return 0
