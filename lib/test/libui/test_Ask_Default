#!/usr/bin/env libui
#####
#
# test_Ask_Default
#
#####

test_Ask_Default () {
  LibuiGetDisplayTestValues
  Yes -e
  TERMINAL=true LibuiPerformTest 'Ask -d "default" "Test Ask with default?"'
  local tv=${?}
  Yes -E
  LibuiValidateTest "${tv}" 0 'Test Ask with default? [default] default'
  return ${?}
}

# test setup
_Test_opt+='-x P '

return 0
